# Code generated by protocolgen; DO NOT EDIT.
# Source: backend/internal/protocolschema/schemas/aap/v1
# Schema-set SHA-256: b831521775532562decbaef0f79ebc82d53d6d10ca4be7afab81390d2c418d27

components:
  schemas:
    AAPSpecVersion:
      type: string
      enum: ["1.0"]
    AAPProtocolDate:
      type: string
      enum: ["2026-08-11"]
    AAPEventType:
      type: string
      description: Frozen v1 persisted protocol event types from the Schema Registry.
      enum:
        - run.accepted
        - run.started
        - run.waiting
        - run.resumed
        - run.completed
        - run.failed
        - run.cancelled
        - item.started
        - item.delta
        - item.completed
        - interaction.requested
        - interaction.resolved
        - interaction.expired
        - usage.updated
    AAPRunStatus:
      type: string
      enum:
        - accepted
        - running
        - waiting_interaction
        - completed
        - failed
        - cancelled
    AAPRunTrigger:
      type: string
      enum:
        - message
        - api
        - workflow
        - system
    AAPItemStatus:
      type: string
      enum:
        - in_progress
        - waiting
        - completed
        - failed
        - declined
        - cancelled
    AAPDeltaType:
      type: string
      enum:
        - text_delta
        - arguments_json_delta
        - output_delta
        - progress
    AAPEventEnvelope:
      type: object
      additionalProperties: false
      required:
        - specVersion
        - type
        - eventId
        - streamId
        - sequence
        - occurredAt
        - workspaceId
        - agentId
        - conversationId
        - runId
        - traceId
        - data
      properties:
        specVersion:
          $ref: '#/components/schemas/AAPSpecVersion'
        type:
          type: string
        eventId:
          type: string
          format: uuid
        streamId:
          type: string
          pattern: '^run:'
        sequence:
          type: integer
          format: int64
          minimum: 1
        occurredAt:
          type: string
          format: date-time
        workspaceId:
          type: string
          format: uuid
        agentId:
          type: string
          format: uuid
        conversationId:
          type: string
          format: uuid
        runId:
          type: string
          format: uuid
        traceId:
          type: string
        data:
          type: object
