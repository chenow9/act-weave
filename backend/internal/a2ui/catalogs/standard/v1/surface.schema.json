{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://catalog.actweave.dev/standard/v1/surface.schema.json",
  "title": "ActWeave A2UI Surface v1",
  "description": "Body of an a2ui content part surface. Mirrors the A2UI createSurface payload so a conforming client can feed it to an official renderer unchanged. surfaceId and catalogId are injected by the platform after validation; models never emit them.",
  "type": "object",
  "properties": {
    "surfaceId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9_:.-]+$"
    },
    "catalogId": {
      "const": "https://catalog.actweave.dev/standard/v1/catalog.json"
    },
    "components": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "items": { "$ref": "catalog.json#/$defs/anyComponent" }
    },
    "dataModel": {
      "type": "object",
      "description": "Root state referenced by DataBinding paths. Opaque beyond being an object."
    }
  },
  "required": ["components"],
  "additionalProperties": false
}
