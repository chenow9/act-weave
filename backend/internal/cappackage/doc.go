// Package cappackage is the ActWeave-native Tool and Workflow config file.
//
// A package is a YAML document (actweave.package/v1) that round-trips
// governed Tool drafts and Workflow drafts. It never contains secrets,
// never auto-publishes, and identifies resources by slug rather than UUID.
package cappackage
