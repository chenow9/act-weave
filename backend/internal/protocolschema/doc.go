// Package protocolschema owns the public, transport-independent schemas for
// the ActWeave Agent Access Protocol. Transport adapters must encode the same
// event envelope; they must not define their own business event vocabulary.
package protocolschema
